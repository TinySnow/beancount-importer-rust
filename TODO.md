# TODO


error

rule

providers

model/mod

runtime/config_loader 下的某些函数是否应该迁移到 util 里面去？

如果 csv 和 spreadsheet 文件夹下只有 mod 文件，是否应该采用直接命名的方式（也即 csv.rs）的形式？

现在有很多 default_ 在配置文件里面，是否可以统一写成 default: 模块？

因为目前该项目仅我自己使用，所以不必保留旧兼容，直接使用最佳实践即可，不必保持 api 稳定

